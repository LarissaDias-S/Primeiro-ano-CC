#include <stdio.h>
 
int main()
{
    int i=1, j=7, x, y;

    for(y = 0; y < 5; y++)
    {
        for(x = 0; x < 3; x++)
        {
            printf("I=%d J=%d\n", i, j - x);
        }
        i = i + 2;
    }


    return 0;
}
