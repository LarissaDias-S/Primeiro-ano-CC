#include <stdio.h>
#include <stdlib.h>
 
 int multi(int x, int y){
     
     if((x % y) == 0 || (y % x) == 0){
         return 1;
     }
     
     else {
         return 0;
     }
 }
 
int main() {
 int a, b, r;
 scanf("%d %d", &a, &b);
 
 r = multi(a, b);
 
 if (r == 1) {
     printf("Sao Multiplos\n");
 }
 else{
     printf("Nao sao Multiplos\n");
 }
 
    return 0;
}
