#include <stdio.h>
 
int main()
{
  int c, p, f;

  scanf("%d\n %d\n %d", &c, &p, &f);

 if ((1<= c <= 1000) &  (1<= p <= 1000) &  (1<= f <= 1000)) {
    if((p/f) >= c ){
        printf("S\n");
    }
    else{
        printf("N\n");
    }
}
 else {
    return 0;
 }
}
