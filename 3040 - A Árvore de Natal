#include <stdio.h>
 
int main()
{
    int N, a, d, g;

    scanf("%d", &N);

    for(int i = 1; i <= N; i++)
    {
        scanf("%d %d %d", &a, &d, &g);

        if((a >= 200) && (a <= 300) && (d >= 50) && (g >= 150))
        {
            printf("Sim\n");
        }
        else
        {
            printf("Nao\n");
        }
    }


    return 0;
}
