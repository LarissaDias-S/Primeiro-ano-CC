#include <stdio.h>
#include <stdlib.h>
 
int main() {
 int t, s[5], i, cont=0;
 
 scanf("%d", &t);
 
 for(i=0; i < 5; i++){
     scanf("%d", &s[i]);
     
     if(s[i] == t){
         cont++;
     }
 }
 
 printf("%d\n", cont);
 
    return 0;
}
