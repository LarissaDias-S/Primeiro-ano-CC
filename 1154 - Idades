#include <stdio.h>
#include <stdlib.h>

int
main ()
{
  double m = 0.0, media;
  int n, i = 0;

  scanf ("%d", &n);

  while (n >= 0.0)
    {
      

      if (n >= 0.0)
	{
	  m = m + n;

	  i++;
	}
	
	scanf ("%d", &n);

    }

  media = m / i;

  printf ("%.2lf\n", media);


  return 0;
}
