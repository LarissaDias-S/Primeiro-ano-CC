#include <stdio.h>
 
int main()
{
    int C, N;

    scanf("%d", &C);

    for(int i = 1; i <= C; i++)
    {
        scanf("%d", &N);

        if(N <= 8000)
        {
            printf("Inseto!\n");
        }
        else
        {
            printf("Mais de 8000!\n");
        }
    }

    return 0;
}
