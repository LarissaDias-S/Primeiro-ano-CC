#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int N, lamp, x=0;
    char alfabeto[26], mensagem[26];

    while (scanf("%s", alfabeto) != EOF) {
    scanf("%d", &N);

    for(int i = 0; i < N; i++)
    {

        scanf("%d", &lamp);

                printf("%c", alfabeto[lamp-1]);


    }
            printf("\n");

    }


    return 0;
}

