#include <stdio.h>
#include <stdlib.h>
 
int main() {
 int i=0, x, z, somax=0;
 
 scanf("%d", &x);
 
 scanf("%d", &z);
 while(z <= x){
  scanf("%d", &z);
 }
 
 while(somax <= z){
     
     somax = somax + x + i;
     
     i++;
 }
 
 printf("%d\n", i);
 
 
    return 0;
}
