#include <stdio.h>
 
int main()
{
   int i, n, qpar=0;


   for ( i = 1; i <= 5; i++) {

    scanf("%d", &n);


     if(n % 2 == 0) {

     qpar++;

     }

   }

    printf("%d valores pares\n", qpar);

   return 0;
}
