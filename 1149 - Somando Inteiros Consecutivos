#include <stdio.h>
#include <stdlib.h>
 
int main() {
int a, n=0, i, somai=0, ai;

scanf("%d", &a);

while(n <= 0){
    scanf("%d", &n);
}

for(i=0; i <= n; i++){

  if((0 <= i) && (i <= (n - 1))){
    ai = a + i;
     somai += ai;
  }  
    
}
printf("%d\n", somai);
 
    return 0;
}
